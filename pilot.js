const now = new Date();
const day = now.getDay(); // Get the current day (0 = Sunday, 1 = Monday, etc.)
const hour = now.getHours(); // Get the current hour (0-23)

console.log(now);
console.log(day);
console.log(hour);
